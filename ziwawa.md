# 玩家模型正视面显示功能

## 功能概述

此功能在Minecraft游戏屏幕左上角显示玩家当前MMD模型的正面视图，实时展示玩家的动作状态。

## 功能特性

- **实时显示**：在屏幕左上角显示玩家当前加载的MMD模型
- **正面视角**：模型始终以正面朝向观察者，便于查看动作细节
- **动作同步**：实时反映玩家的当前动作状态（行走、跑步、跳跃等）
- **可配置**：可通过模组设置启用或禁用此功能
- **性能友好**：使用优化的渲染方式，对游戏性能影响极小

## 使用方法

### 启用功能

1. 打开游戏，进入任意世界
2. 按 `Esc` 打开暂停菜单
3. 点击 "模组设置" 或 "Mod Settings"
4. 找到 "调试设置" 或 "Debug Settings" 分类
5. 启用 "玩家模型正视面" 或 "Player Front View" 选项
6. 保存设置并返回游戏

### 查看效果

启用后，屏幕左上角会出现一个半透明黑色背景的窗口，显示：
- 玩家当前使用的MMD模型名称（顶部）
- 模型的正面视图（中央）
- 实时的动作动画

### 禁用功能

如需禁用，重复上述步骤，取消勾选 "玩家模型正视面" 选项即可。

## 技术实现

### 核心组件

1. **配置系统**
   - `ConfigData.java`: 添加 `playerFrontViewEnabled` 配置项
   - `IRenderConfig.java`: 添加配置接口方法
   - `ConfigManager.java`: 提供配置读取入口

2. **HUD渲染器**
   - `PlayerFrontViewHud.java`: 核心渲染逻辑
   - 使用Minecraft的GuiGraphics进行2D overlay渲染
   - 通过PoseStack进行3D变换和模型渲染

3. **平台集成**
   - Fabric: 在 `FabricClientRuntimeHooks.java` 中注册HudRenderCallback
   - NeoForge: 在 `NeoForgeClientRuntimeHooks.java` 中注册RenderGuiEvent

4. **国际化支持**
   - 中文 (zh_cn.json)
   - 英文 (en_us.json)
   - 日文 (ja_jp.json)

### 渲染流程

1. 检查配置是否启用
2. 获取本地玩家实例
3. 解析玩家当前使用的MMD模型
4. 在屏幕左上角绘制半透明背景
5. 设置3D变换矩阵（缩放、旋转）
6. 调用模型渲染API绘制正面视图
7. 显示模型名称标签

## 注意事项

- 此功能仅在玩家已加载MMD模型时生效
- HUD会在隐藏GUI或显示调试屏幕时自动隐藏
- 模型渲染使用固定亮度，确保在任何光照条件下都清晰可见
- 默认情况下此功能是禁用的，需要手动启用

## 故障排除

### HUD不显示

1. 确认已在设置中启用该功能
2. 确认玩家已选择并加载了MMD模型
3. 确认没有按下F1隐藏GUI
4. 检查是否有其他模组覆盖了HUD渲染

### 模型显示异常

1. 尝试重新加载模型
2. 检查模型文件是否完整
3. 确认模型格式兼容（PMX/VRM）

## 开发者信息

如需自定义HUD样式，可修改以下常量：
- `HUD_SIZE`: HUD窗口大小（默认120像素）
- `PADDING`: 屏幕边距（默认10像素）
- `MODEL_SCALE`: 模型缩放比例（默认0.8）
- `BG_COLOR`: 背景颜色（默认半透明黑色）

## 许可证

本功能遵循项目主许可证。
