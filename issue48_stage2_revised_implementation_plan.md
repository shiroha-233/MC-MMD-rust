# Issue #48 第二阶段修订实施计划

## 1. 问题重新定义

当前实现按静止姿态保留后脑约 25%，再删除其余头部三角形。该方案无论静态还是动态更新，都会制造一个开放网格边界；当奔跑前倾、特殊动作或相机与骨骼存在一帧差异时，玩家看到的就是这个边界形成的切口。因此第二阶段不能继续调整“保留后脑比例”，而应保证相机可见区域内不存在开放边界。

第一阶段已经验证完成，本阶段不重新检测、不回退其场景隔离、背包跟随或现有第一人称渲染入口。

## 2. 最终方案

采用“静态头部主体剔除 + 本帧相机空间补充剔除”的两层规则。

第一层在模型加载时预计算头部候选集合。头骨及其后代骨骼的累计权重仍用于识别脸、头皮、眼睛、发片等明确属于头部的三角形，但不再保留固定比例的后脑外壳。明确属于头部主体的三角形在第一人称中完整剔除，从拓扑上移除当前可见切口的主要来源。

第二层只在 `RenderScene.FIRST_PERSON` 的单次 Draw 中执行。模型完成本帧动画更新后，Java 从最终 `deliverStack.last().pose()` 取得与实际绘制一致的 model-view 矩阵，Rust 使用当前姿态的候选顶点在相机视空间内判断遮挡。除头部主体外，靠近眼位并进入“扩张后的屏幕视锥”的后脑边缘、发片及颈后过渡三角形也会被剔除。扩张边距用于让开放边界停留在屏幕外，而不是刚好贴着画面边缘。

动态补充区域必须同时满足以下限制：

1. 三角形属于头部候选或头颈过渡候选，不能对全身网格做无限视锥删除。
2. 三角形位于相机附近的头颈深度范围，避免删除远处躯干。
3. 三角形与扩张后的视锥相交即剔除，不能只检查“三个顶点全部在内”，否则跨越边界的大三角形仍会留下切口。
4. 无法取得有效矩阵、候选集合为空或结果异常时，回退到完整剔除头部主体；不得回退到保留后脑外壳的旧规则。

头颈过渡候选优先通过“头部三角形的有限拓扑邻接环 + 静止姿态空间约束”生成。这样可以覆盖低头部权重的后颈接缝，又不会仅因某个身体材质与头部共用材质而误删胸口或四肢。

## 3. 双眼锚点与相机时序修正

实机截图确认，头骨中心不能作为桌面第一人称相机位置。它位于双眼中点的后下方，低头时会让视点落入被裁掉的头部内部，并从颈部开放边界看见身体。相机锚点必须恢复为动画后左右眼骨骼的世界位置中点；只有缺少双眼骨骼时才回退到单眼；完全没有眼骨时回退原版相机。

奔跑前倾仍有问题的另一原因是帧内时序：`Camera.setup` 早于本地模型本帧的 `syncModelState()`、动画和物理更新，原实现只能读取上一帧渲染后的眼位。动态裁切可以隐藏局部表面，但不能修复上一帧相机与本帧骨骼之间的位置差。

修订后的流程是在 `GameRenderer.renderLevel` 开始、`Camera.setup` 之前，为本地桌面第一人称模型执行一次相机姿态预更新：同步本帧动作与实体姿态，推进一次动画/物理，立即缓存本帧双眼中点。模型实例记录该姿态已经准备完成，后续阴影和世界 pass 不再推进；正式 `RenderScene.FIRST_PERSON` Draw 复用并消费同一姿态。这样相机与实际绘制模型严格来自同一次求值，也不会重复推进 Bullet 物理。

预更新只作用于本地桌面第一人称。VR、背包、远端玩家和第三人称保持原更新路径；预更新失败时回退原版相机，不阻断渲染。

## 4. 坐标与数据流

Java 端在应用实体 yaw、pitch、translation 和最终模型缩放后复制 model-view 矩阵，并连同当前投影矩阵传给 native。Rust 将本帧蒙皮后的候选顶点变换到裁剪空间，通过齐次坐标执行扩张视锥相交测试。

CPU 蒙皮后端直接使用 `update_positions`。GPU 蒙皮后端不从 GPU 回读顶点；Rust 只对预计算的少量头部及头颈候选顶点复用现有骨骼矩阵、权重与 morph 数据进行局部 CPU 蒙皮。两种后端最终进入同一个视锥判定函数，避免产生两套裁切行为。

JNI 使用 Java 提供的 direct buffer 复制动态索引和对应 submesh 范围，不长期暴露可在下一次重建时失效的 Rust `Vec` 指针。动态索引数量不会超过原始索引数量，因此 Java 初始化时按原始 EBO 容量分配 `GL_DYNAMIC_DRAW` 缓冲，更新时使用 `glBufferSubData`。

## 5. 缓存与性能

动态 EBO 以模型实例为单位缓存。只有以下任一条件变化时才重建：

1. `nativeUpdateRevision` 变化，即动画、物理或 morph 姿态变化。
2. model-view 或投影参数变化超过小阈值，即相机位置、朝向、FOV 或窗口比例发生有效变化。
3. 第一人称候选数据首次初始化。

候选顶点和候选三角形在加载阶段预计算。每帧不扫描全模型，不创建新的大型临时数组；动态结果复用已有容量。

## 6. 场景与多人兼容边界

动态索引只由 `RenderScene.FIRST_PERSON` 请求，并保存在本地模型实例对应的 native handle 与 Java renderer 实例中。

远端玩家、第三人称、本地背包、GUI、Iris 阴影、普通世界阴影和非眼部 VR pass 继续使用原始 EBO。动态查询不修改持久化的全局 first-person 标志，也不让一次 Draw 的状态泄漏到后续 Draw，因此多人游戏不需要新增网络包或同步字段。

Fabric 与 Forge 共用 common/native 的动态裁切实现。平台 CameraMixin 的现有差异不在本阶段顺带修改；矩阵输入来自最终绘制路径，不依赖两端各自重算相机方向。

## 7. 实施顺序

1. 在 Rust 中重构第一人称候选预计算：移除后脑保留比例，加入完整头部主体和受限头颈邻接候选，并补齐纯几何单元测试。
2. 增加基于当前姿态、model-view 与 projection 的扩张视锥相交函数，测试前倾、相机侧偏、跨视锥大三角形和异常矩阵回退。
3. 扩展 JNI 与 common bridge，使用 direct buffer 输出本次动态索引及 submesh 数据。
4. 修改 OpenGL CPU 与 GPU renderer，在第一人称 Draw 前按 revision 和矩阵变化刷新 `GL_DYNAMIC_DRAW` EBO。
5. 恢复动画后双眼中点相机锚点，并在 `renderLevel` 开始阶段加入本地第一人称姿态预更新；正式 Draw 通过实例帧标记复用同一姿态，禁止重复推进动画和物理。
6. 验证 Fabric/Forge 编译、Rust 测试、CPU/GPU 蒙皮、普通/Toon/Iris 路径，并由开发环境实测正常站立、奔跑前倾、特殊奔跑动作、上下左右转头、背包、阴影和多人远端玩家。

## 8. 验收标准

正常第一人称不再看到头部切口、脸、眼睛或发片遮挡。

身体大幅前倾或特殊奔跑动作时，画面中心及边缘不出现后脑开放切面；后颈进入相机近处时也不会直接遮挡视野。

低头观察身体时，胸口、肩膀和手臂不会因动态视锥被大面积误删。第三人称、背包和远端玩家保持完整头部，Iris 阴影仍使用完整模型。

CPU 与 GPU 蒙皮的裁切结果一致，动态更新不产生持续分配、明显卡顿或 EBO 越界。



