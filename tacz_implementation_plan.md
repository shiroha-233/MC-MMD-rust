# TaCZ 第一人称分类持枪动作方案（阶段一，待审批）

## 重新界定问题

当前问题的直接原因不是 TaCZ 手部锚点没有接入 MMD，而是现有动画状态机只在 `isUsingItem()`、挥手或受伤时更新手部覆盖层。玩家只是拿着 TaCZ 枪械时，MMD 仍播放普通 `idle/walk` 与默认物品持有姿势。

第一阶段只修正“持有什么，就采用什么基础持有姿势”：识别 TaCZ 枪械类别，并持续播放对应的上半身持枪 VMD。第一阶段不接管 TaCZ 的枪械节点、相机节点或逐帧手部动画。

目标平台为 Minecraft 1.20.1。实现建立在提交 `4242799` 的第一人称修复基础上。

## 第一阶段范围

第一阶段增加四种基础姿势槽位：

```text
tacz_hold_pistol       手枪
tacz_hold_rifle        步枪
tacz_hold_machine_gun  机枪及重型自动武器
tacz_hold_generic      已确认是 TaCZ 枪械，但类别未知时的回退
```

这些动作只覆盖上半身持枪相关骨骼，并在动画层中循环播放。下半身继续使用现有待机、行走、奔跑、下蹲等动作，因此拿枪移动时不需要为每种移动状态再复制一份 VMD。

本阶段不实现：

- `lefthand_pos/righthand_pos` 到 MMD 手臂的逐帧跟随；
- TaCZ 换弹、拉栓、检视和后坐力对 MMD 骨骼的精确驱动；
- 第三人称专用动作；
- 远端玩家和联机同步；
- 逐枪坐标或逐枪 VMD。

## 枪械分类

分类器输出 `PISTOL`、`RIFLE`、`MACHINE_GUN`、`GENERIC_GUN` 或 `NONE`。

识别顺序如下：

1. 优先读取 TaCZ 1.20.1 已有的枪械类型或枪械数据字段；具体 API/字段名在实现前以目标版本源码确认，不在计划中猜测。
2. 若 TaCZ 数据没有可用类别，则读取兼容层定义的数据包物品标签，允许整合包作者覆盖分类。
3. 已确认是 TaCZ 枪械但类别无法归类时，使用 `GENERIC_GUN`。
4. 非 TaCZ 物品返回 `NONE`，完全保持现有动画行为。

数据包覆盖标签建议采用：

```text
mmdskin:tacz_pistols
mmdskin:tacz_rifles
mmdskin:tacz_machine_guns
```

标签只作为覆盖和补充，不要求每个枪包重复维护一份分类。分类逻辑放在可选 TaCZ 兼容端口中，common 动画状态机不直接引用 TaCZ 类；未安装 TaCZ 时端口返回 `NONE`。

## 动画状态

现有第 0 层继续负责 `idle/walk/sprint/sneak` 等身体动作。持枪动作使用第 1 层，并设置上半身骨骼蒙版。

状态优先级：

```text
死亡、睡眠等强制状态
→ 现有明确的使用/挥动动作
→ TaCZ 分类持枪循环
→ 空闲覆盖层
```

也就是说，玩家只要主手持有 TaCZ 枪械且没有更高优先级动作，第 1 层就保持对应持枪 VMD，而不是被清空成普通 `Idle`。切换枪械类别、放下枪械或切到普通物品时，以现有过渡系统平滑切换。

第一版不依据瞄准、开火和换弹状态切换更多 VMD。这些状态仍由 TaCZ 的枪械模型动画表现，避免把简单持枪修复重新扩大成完整动作桥接。

## VMD 内容

三份分类 VMD 只表达基础姿势差异：

- 手枪：双手靠近，肘部收拢，武器轴线偏高；
- 步枪：双手前后分离，托枪姿势较稳定；
- 机枪：双臂展开幅度和承重感更明显，避免沿用步枪的小幅姿势；
- 通用回退：采用保守的双手长枪姿势。

VMD 不记录枪械位置、相机位置、扳机或拉机柄坐标。动作资源仍走现有 `AnimationLibrary`，允许模型目录或动画映射覆盖默认 VMD，方便不同体型模型自行调整。

默认 VMD 的实际骨骼角度不能凭文字填充。动作文件由用户制作或提供后放入默认动画资源；在动作尚未提供时，可以先用占位测试 VMD 验证分类、循环、蒙版和过渡，但不能把占位动作当作最终成品。

## 最小实现范围

预计涉及以下职责，不引入第一人称网格拆分或 Rust 坐标改造：

1. 新增独立的枪械姿势分类接口及 TaCZ 可选实现。
2. 修改 `AnimationStateManager`，让第 1 层在“持枪但未使用物品”时保持分类持枪动作。
3. 使用现有 `setLayerBoneMask` 为持枪覆盖层限制上半身/手臂骨骼，并在离开持枪状态时恢复。
4. 在默认动画提取清单中登记四份 VMD。
5. 添加分类优先级、状态切换、资源缺失回退和未安装 TaCZ 的单元测试。

若 Fabric 与 Forge 无法通过同一种无硬依赖方式读取 TaCZ 分类，平台差异只留在各自兼容适配器中，不复制动画状态机。

## 回退规则

- 分类专用 VMD 缺失：依次回退 `tacz_hold_generic`，再回退空闲覆盖层；
- TaCZ 枪械类别缺失：使用 `GENERIC_GUN`，不按物品 ID 文本猜测；
- 未安装 TaCZ：不加载 TaCZ 类，所有物品保持当前行为；
- 副手持枪：第一阶段不启用双枪专用姿势，保持现有逻辑；
- 自定义动画、舞台动画和 VR：优先级高于分类持枪动作，不被覆盖。

## 第一阶段验收

1. 主手分别持手枪、步枪、机枪时，MMD 使用不同的循环持枪姿势。
2. 站立、行走和下蹲时，上半身持枪覆盖与下半身基础动作可以同时工作。
3. 切枪、放下枪和切换类别时过渡平滑，不残留上一类动作。
4. 未分类 TaCZ 枪械使用通用持枪姿势，而不是普通物品挥手姿势。
5. 普通物品、第一人称相机、库存预览、VR 和现有自定义动作没有回归。
6. 第一阶段不产生第三人称专用逻辑或联机协议变更。

## 第二阶段

第一阶段达到稳定可用后，再单独评估是否需要将 TaCZ 的瞄准、开火、换弹、拉栓和检视状态映射为额外 MMD 动作，以及是否接入 `lefthand_pos/righthand_pos`。第三人称和联机多人仍放在第二阶段之后独立设计。

## 审批建议

建议先采用“TaCZ 枪械分类器 + 三类持枪 VMD + 通用回退 + 上半身动画层”的最小方案。它直接修复当前把枪当普通物品的问题，也保留以后追加精确手部跟随的空间。
